<% content_for :head do %>
  <%= javascript_include_tag "http://maps.googleapis.com/maps/api/js?sensor=false" %>
  <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.6.4/jquery.min.js" %>
  <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.16/jquery-ui.min.js" %>
  <script type="text/javascript">
  <!--
    $(document).ready(function(){
      var geocoder = new google.maps.Geocoder();
      var latlng = new google.maps.LatLng(39.529,-119.814);
      var myOptions = {
        zoom: 14,
        center: latlng,
        mapTypeId: google.maps.MapTypeId.ROADMAP,
        mapTypeControlOptions: {
          style: google.maps.MapTypeControlStyle.DROPDOWN_MENU,
          position: google.maps.ControlPosition.TOP_LEFT,
        },
        panControl: false,
        zoomControlOptions: {
          style: google.maps.ZoomControlStyle.DEFAULT,
        },
      };
      var map = new google.maps.Map(document.getElementById("map_canvas"), myOptions);
      var infowindow;

      $("#search-form > form").submit(function(){
        var address = $("#search").val();
        geocoder.geocode( { 'address': address}, function(results, status) {
          if (status == google.maps.GeocoderStatus.OK) {
            latlng = results[0].geometry.location;
            map.panTo(latlng);
            var marker = new google.maps.Marker({
              map: map,
              position: results[0].geometry.location,
            });
          } else {
            alert("Geocode was not successful for the following reason: " + status);
          }
        });
        $.ajax({
          url: 'https://api.foursquare.com/v2/venues/explore?ll=' + latlng.toUrlValue(3) + '&client_id=JD1SH050Z32DQ3IRI4ND4J5LKFUEQOOJ1ENBJRQAGR32TWSJ&client_secret=1RQDNQYUXO3F5OR5SD0JAE1LZHBTBNY0EON3JR2OSGUEL1MB',
          success: function(data) {
            var foursquare = data.response.groups[0].items;
            var hottest = 0;
            var therenow = -1;
            for (var i = 0; i < foursquare.length; i++) {
              if (foursquare[i].venue.hereNow.count > therenow) {
                hottest = i;
              }
            }

            var contentString = '<div id="content">'+
                '<h1 id="firstHeading" class="firstHeading">' + foursquare[hottest].venue.name + '</h1>'+
                '<div id="bodyContent">'+
                '<p><b>Uluru</b>, also referred to as <b>Ayers Rock</b>, is a large ' +
                'sandstone rock formation in the southern part of the '+
                'Northern Territory, central Australia. It lies 335 km (208 mi) '+
                'south west of the nearest large town, Alice Springs; 450 km '+
                '(280 mi) by road. Kata Tjuta and Uluru are the two major '+
                'features of the Uluru - Kata Tjuta National Park. Uluru is '+
                'sacred to the Pitjantjatjara and Yankunytjatjara, the '+
                'Aboriginal people of the area. It has many springs, waterholes, '+
                'rock caves and ancient paintings. Uluru is listed as a World '+
                'Heritage Site.</p>'+
                '<p>Attribution: Uluru, <a href="http://en.wikipedia.org/w/index.php?title=Uluru&oldid=297882194">'+
                'http://en.wikipedia.org/w/index.php?title=Uluru</a> (last visited June 22, 2009).</p>'+
                '</div>'+
                '</div>';

            if (infowindow) {
              infowindow.close();
            }
            infowindow = new google.maps.InfoWindow({
                content: contentString,
                maxWidth: 200,
                position: new google.maps.LatLng(foursquare[hottest].venue.location.lat, foursquare[hottest].venue.location.lng),
            });

//            var marker = new google.maps.Marker({
//                position: new google.maps.LatLng(foursquare[hottest].venue.location.lat, foursquare[hottest].venue.location.lng),
//                map: map,
//                title: foursquare[hottest].venue.name,
//            });

            infowindow.open(map);

            $("#overlay").animate({height: "150px"});
            $("#search-form").css("margin-top", "15px");
          },
        });
        return false;
      });
    });
  // -->
  </script>
<% end %>

<div id="map_canvas" style="width:100%; height:100%;"></div>
<div id="overlay">
  <div id="outer">
    <h1>The WHAT</h1>
    <div id="middle">
      <div id="search-form">
        <form class="ui-form">
          <input type="text" id="search" name="search" value="Reno, NV" size="20" />
          <input type="submit" value="Go" />
        </form>
      </div>
    </div>
  </div>
</div>
